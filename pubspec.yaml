name: point_of_sales
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  fluid_bottom_nav_bar: ^1.4.0

  home:
    path: features/home

  product_list:
    path: features/product_list

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
