name: point_of_sales
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  auth:
    path: features/auth

  bottom_navigation:
    path: features/bottom_navigation

  add_product:
    path: features/add_product

  add_cashier:
    path: features/add_cashier

  transaction:
    path: features/transaction

  common:
    path: core/common

  ui:
    path: core/ui

  dependencies:
    path: core/dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
